apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: learning-app-ingress
spec:
  rules:
  - host: learning.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-service
            port:
              number: 80
